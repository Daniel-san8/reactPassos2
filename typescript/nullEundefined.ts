const button = document.querySelector("button");
const config = localStorage.getItem("config");

if (button !== null) true;

if (button) {
  button.click();
}

button?.click();

let totall;

if (totall) {
  console.log(`^${totall} foi definido`);
}
